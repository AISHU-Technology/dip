openapi: 3.1.0
info:
  title: DIP HUB 内部接口
  version: 1.0.0

servers:
  - url: /api/internal/dip-hub/v1/

paths:
  # 引用健康检查的 path 定义
  /healthz:
    $ref: "./health/health.paths.yaml#/paths/healthz"
  /readyz:
    $ref: "./health/health.paths.yaml#/paths/readyz"

components:
  schemas:
    # 引用健康检查的 schema 定义
    HealthStatus:
      $ref: "./health/health.schemas.yaml#/components/schemas/HealthStatus"
    HealthCheckItem:
      $ref: "./health/health.schemas.yaml#/components/schemas/HealthCheckItem"
